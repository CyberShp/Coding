[pytest]
testpaths =
    tests
    agent/tests

asyncio_mode = auto
timeout = 30

markers =
    slow: slow-running tests (cross-module or heavy setup)
    ssh: tests involving SSH connection behavior (mocked or integration-like)
    integration: integration tests spanning API + db/core flow
    e2e: end-to-end workflow tests
